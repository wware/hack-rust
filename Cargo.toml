[package]
name = "bohemia_graph"
version = "0.1.0"
edition = "2024"

[lib]
name = "bohemia_graph"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "bohemia_query"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
